// 이 파일을 secrets.h 로 복사한 뒤 값을 채워 넣으세요.
// secrets.h 는 .gitignore 에 들어 있어 저장소에 올라가지 않습니다.
#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"

// 무선 펌웨어 업데이트(OTA)용. 같은 네트워크의 아무나 펌웨어를 덮어쓰지
// 못하게 비밀번호를 건다.
#define OTA_HOSTNAME "esp32-radio"
#define OTA_PASSWORD "change-me"
